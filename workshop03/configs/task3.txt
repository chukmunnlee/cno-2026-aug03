
sum(k6_http_reqs_total) by (scenario)

sum(http_server_duration_count) by (http_route)

(sum(cache_count_total{hit="true"}) by (cloud_region) 
   / sum(cache_count_total) by (cloud_region)) * 100

sum(rate(cache_count_total[1m])) by (hit)

histogram_quantile(
   0.9, 
   sum(rate(http_server_duration_milliseconds_bucket[5m])) by (le, service_name)
)
histogram_quantile(
   0.99, 
  sum(rate(http_server_duration_bucket[5m])) by (le, service_name, http_route)
)

client avg duration =
   avg(k6_http_req_duration_milliseconds_sum / k6_http_req_duration_milliseconds_count) by (service_name)
server processing avg latency = 
   avg(http_server_duration_milliseconds_sum{http_route="/"}/ http_server_duration_milliseconds_count{http_route="/"})
Network latency = client avg latency - server processing avg latency